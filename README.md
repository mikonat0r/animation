CSS Football Movie — The Last Goal ⚽

Мини-мультик, полностью реализованный только на HTML и CSS, без использования JavaScript.
Проект представляет собой анимированную сцену футбольного матча с сюжетом: решающий гол на последних секундах.

🎬 Идея проекта

Это визуальный рассказ длиной ~1 минута:

ночной стадион,

движение «камеры» по полю,

атака синей команды,

падение защитника,

удар в «девятку»,

неудачный прыжок вратаря,

гол, смена счёта и финальная победа.

Анимация проигрывается автоматически при открытии страницы и является цельной историей, а не набором отдельных эффектов.

🛠 Технологии

HTML5

CSS3

❌ JavaScript не используется

Используются:

@keyframes

animation

transition

CSS-переменные (:root)

transform, opacity, scale, rotate

относительные единицы (vw, vh) для эффекта «камеры»

🎥 Как работает анимация

Вся сцена помещена в контейнер .camera-rig шириной 400vw

Анимация cameraLogic имитирует движение камеры слева направо

Все события (бег игроков, полёт мяча, гол, табло, финальные титры)
синхронизированы одной временной шкалой через --master-duration: 60s

Счёт меняется с 3:3 на 4:3 с помощью CSS-анимации (score-flip)

Удар по мячу и реакция сетки реализованы без JS, только через @keyframes

📁 Структура проекта
/project
 ├── index.html   // HTML-разметка сцен и объектов
 ├── style.css    // Вся логика анимаций и визуала
 └── README.md    // Описание проекта

✅ Соответствие заданию

✔ Анимация длительностью ~1 минута

✔ Мини-мультик с сюжетом и логикой

✔ Только HTML + CSS

✔ Используются @keyframes и animation

✔ Автоматический запуск при открытии страницы

✔ Цельная история, а не набор эффектов

🏁 Итог

Проект демонстрирует, что CSS может быть не только стилями, но и языком анимационного повествования.
Без JavaScript, без canvas — только чистая логика, тайминг и воображение.
